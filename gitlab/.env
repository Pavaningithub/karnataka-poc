# GitLab Configuration - Generic Setup
# This configuration works with any user and directory structure
# Volumes are created as named volumes to avoid permission issues

# Project Configuration
PROJECT_NAME=gitlab-selfhosted
COMPOSE_PROJECT_NAME=gitlab-selfhosted

# Network Configuration
GITLAB_HTTP_PORT=8929
GITLAB_SSH_PORT=2222

# GitLab Configuration
# External URL for browser access
GITLAB_EXTERNAL_URL=http://localhost:${GITLAB_HTTP_PORT}
# Internal hostname for container-to-container communication  
GITLAB_INTERNAL_HOSTNAME=gitlab

# GitLab Runner Configuration
RUNNER_TOKEN=""
RUNNER_NAME=gitlab-runner-shell

# Optional: Custom domain configuration
# Uncomment and modify for custom domain setup:
# GITLAB_EXTERNAL_URL=http://your-domain.com
# GITLAB_HTTP_PORT=80
